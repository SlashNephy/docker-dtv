name: mirakurun-client

on:
  push:
    paths:
      - "src/mirakurun-client/**"

  workflow_dispatch:

permissions:
  contents: read
  packages: write
  actions: read
  security-events: write

jobs:
  build:
    uses: SlashNephy/actions/.github/workflows/build-docker-image.yml@3dbec9cde813d07525321de7ba55814cebf26c81 # v0.0.1
    with:
      image-name: ghcr.io/slashnephy/mirakurun-client
      context: src/mirakurun-client
